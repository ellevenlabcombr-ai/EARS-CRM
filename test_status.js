const fetch = require('node-fetch');

async function checkStatus() {
  const url = process.env.EVOLUTION_URL || 'https://www.ears.app.br'; // just guessing or not
  // Actually, I can just write a Next route script to query it using the user's Evolution API.
}
